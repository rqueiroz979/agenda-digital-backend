# Postgres / Supabase connection string (use the pooler if your host needs IPv4):
DATABASE_URL=postgresql://postgres:TX650mt979Banco@aws-1-sa-east-1.pooler.supabase.com:6543/postgres

# Secret key for JWT
SECRET_KEY=uma_chave_super_secreta_aqui

# Token expiration in hours
JWT_EXP_HOURS=8

# Origins allowed by CORS (separados por vírgula)
ALLOWED_ORIGINS=http://localhost:5173,https://agenda-digital-frontend.netlify.app

# Optional: external API timeouts
EXTERNAL_API_TIMEOUT=8
